export { default as ThemeProvider } from './Theme.Context';
export { useTheme, useMergeThemeProps, useComponentDefaultProps, withComponentDefaultProps } from './Theme.Context';

export { defaultTheme } from './Theme.defaults';

export type { ThemeProviderProps, ComponentsPartialProps, PartialThemeDefaultProps } from './Theme.types';
